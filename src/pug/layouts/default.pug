doctype html
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Colors
        link(rel="stylesheet", href="./css/style.css")
        // Ссылка на CSS для Swiper Slider
        link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css")
    body
        .wrapper
            block header
                // Сюда будет вставлена шапка
            main.main
                block content
                    // Сюда будет вставлен основной контент страницы
            block footer
                // Сюда будет вставлен футер

        // Модальные окна
        block modals

        // Скрипт для Swiper Slider
        script(src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js")
        // Основной скрипт
        script(src="./js/main.js")
